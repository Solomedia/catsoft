import styled from '@emotion/styled';
import { space, width, fontSize, color, gridColumn } from 'styled-system';

export const Box = styled.div`
  ${space}
  ${width}
  ${fontSize}
  ${color}
  ${gridColumn}
`;

export const Container = styled.div`
	background: peru;
	padding: 0 15px;
`;
