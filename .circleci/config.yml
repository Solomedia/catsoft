# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15.0

    steps:
      - add_ssh_keys:
          fingerprints:
            - '13:a1:74:7c:be:db:c9:39:21:68:d6:4b:e2:90:fd:69:f9:b5:9c'

      - checkout

      - run:
          name: 'Create Packages'
          command: yarn install

      - run:
          name: 'Create Packages'
          command: yarn build
      - run:
          name: 'Deploy Over SSH'
          command: |
            ssh -o "StrictHostKeyChecking no" circleci@ec2-3-16-168-150.us-east-2.compute.amazonaws.com "rm -rf  /var/www/"
            rsync -avce ssh /working_dir/catsoft/.next/ circleci@ec2-3-16-168-150.us-east-2.compute.amazonaws.com:/var/www/
            fi
